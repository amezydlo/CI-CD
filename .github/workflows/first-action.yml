name: First Workflow
on: workflow_dispatch # Manually trigger

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello worlds"
      - name: Print goodbye
        run: | 
          echo "Done - adios"
          echo "Psst, adios means bye!"
